<h2>商品一覧</h2>

<% @items.each do |item| %>
 <div>
  <%= link_to item_path(item.id) do %>
   <%= attachment_image_tag(item, :image, size: "300x220") %><br>
  <% end %>
  <%=item.name %><br>
  <%=item.price %>
 </div>
<% end %>